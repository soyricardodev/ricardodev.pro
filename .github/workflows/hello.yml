name: Say Hello

on:
  push:
    branches: 
      - main

jobs:
  hello_world:
    runs-on: ubuntu-22.04
    steps:
      - name: Echo a Hello
        run: |
          echo "Hello World"
      - name: Echo the Date
        run: date
      - run: ls -l